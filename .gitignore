# secrets & local
.env
.env.*
!.env.example

# os / editor
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# build / deploy artifacts
dist/
*.zip
# exception: the ready-to-upload portal bundles are committed so they're easy to download
!submit/*.zip
node_modules/

# third-party SDK clone (has its own git repo) — used when we wire multi-portal publishing
/playgama-bridge

# python (factory orchestrator)
factory/__pycache__/
*.pyc
.venv/
